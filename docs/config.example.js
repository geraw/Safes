// Copy this file to config.js after deploying the Cloudflare Worker.
// The static site sends submissions to the Worker. The Worker checks with Paiza
// and writes the submission/result to a Google Form linked to a Google Sheet.
window.CYBER_RIDDLES_CHECKER = "https://cyber-riddles-checker.YOUR_SUBDOMAIN.workers.dev";
